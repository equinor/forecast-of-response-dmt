import React from 'react'
import { DmtSettings } from '../../Types'

const WeatherForecast = (props: DmtSettings): JSX.Element => {
  const { settings } = props

  return <>Cold and wet</>
}

export default WeatherForecast
